import "../styles/components/filter.css"

const Filter = () => {
    return (
        <div className="filter">
            <span>Top</span>
            <span>Flop</span>            
        </div>
    );
};

export default Filter;